# V5.5B-6G0A Prospect Level Production Acceptance — Resumed After G0H

## Status

**ACTIVE / AUTHORIZED FOR CONTROLLED POST-MIGRATION, PRIMARILY READ-ONLY ACCEPTANCE**

G0H proved trusted immutable delivery for the exact reviewed G0F application. This task resumes G0A from current production state; it does not repeat bootstrap, migration, or population writes.

## Required baseline

- Work only in `C:\Users\joshu\Documents\-dynasty-gm-architect` on `feature/manager-intelligence`.
- Read every applicable `AGENTS.md` and all required governance documentation; require a clean tree and matching local/remote HEAD.
- G0I, G0H, G0G, and G0F are complete/checkpointed.
- G0H evidence commit: `892e39653b8d2a8a663cd949373283080f4122d8`.
- Exact immutable G0F target: `f67088c84eb79eab227a4a5759a37269e6c9f631`.
- Pages uses GitHub Actions (`build_type=workflow`).
- Migrations 014/015 are **APPLIED / DO NOT REAPPLY**.
- Prospect schema is `PRESENT`; the existing population contains 5,440 successful rows.

Stop on any repository, deployment, authentication, schema, identity, authorization, or production-state contradiction.

## Purpose

Use canonical G0F diagnostics to determine whether every current provider-driven `UPDATE` is a genuine factual prospect-evidence change. Prove unchanged evidence remains `NO_OP`, representation or collection metadata creates no update, Preview is read-only, conflict/unknown semantics remain intact, and canonical prospect evidence reaches Player Intelligence input.

Historical Preview counts—5,398 exact matches, 24 updates, 5,374 no-ops, and zero invalid/stale/warning/error rows—are reference evidence only. The fresh Preview may differ.

## Authorized sequence

1. Complete repository/governance preflight.
2. Verify and temporarily publish/open exact target `f67088c84eb79eab227a4a5759a37269e6c9f631` through accepted Actions Pages infrastructure.
3. Authenticate normally and select Reddit Phanatics.
4. Read-only verify Migrations 014/015, schema `PRESENT`, and the materially present population.
5. Capture two deterministic `PROSPECT_LEVEL_POPULATION` protected baselines.
6. Run one canonical authenticated Prospect Level Population Preview.
7. Capture provider, identity, level, write-plan, and aggregate reason counts.
8. Inspect every current `UPDATE`; classify each `FACTUAL` or `RESIDUAL FALSE POSITIVE`.
9. Inspect representative `NO_OP`, `CONFLICT`, and `UNKNOWN` rows from every available level category.
10. Require the post-Preview protected comparison to be `UNCHANGED`.
11. Only after diagnostic acceptance passes, run Data Health and inspect canonical Player Intelligence inputs.
12. Decide whether G1 is unblocked for local implementation; do not implement G1.
13. Restore approved normal Pages and verify restoration.
14. Record and checkpoint evidence.

## Critical read-only boundary

Preview and diagnostics are read-only. **Review and Apply are prohibited.** Do not persist factual updates merely to prove diagnostics. Do not apply migrations, import/synchronize, recalculate scores, alter identity/ownership/roster state, or perform unrelated cloud/data writes.

## Update evidence contract

For every `UPDATE`, record stable UUID, exact MLBAM, display-only name, planner action, every reason, stored/incoming `current_level`, `level_source`, `level_availability`, `level_observed_at`, and bounded canonical raw-evidence differences.

Capture aggregate counts for `CURRENT_LEVEL_CHANGED`, `LEVEL_SOURCE_CHANGED`, `LEVEL_AVAILABILITY_CHANGED`, `LEVEL_OBSERVED_AT_CHANGED`, and `LEVEL_RAW_EVIDENCE_CHANGED`. Multi-reason totals may exceed update rows; every contributing reason must be factual.

Accept only genuine canonical level transitions and meaningful canonical source/availability changes. Observation-time changes must be factual—not fetch, collection, Preview, request, job, audit, formatting, or equivalent-timestamp differences. Raw evidence changes must be factual—not key/JSONB/irrelevant-array ordering, primitive formatting, null/missing normalization, or collection/Preview/audit metadata. Any residual false update blocks G0A.

## NO_OP, CONFLICT, and UNKNOWN proof

Inspect representative `NO_OP` rows across each available MLB, AAA, AA, A_PLUS, A, ROOKIE, CONFLICT, and UNKNOWN category. Require no reasons and semantically equivalent evidence. Unchanged factual CONFLICT remains `NO_OP`; changed conflict evidence may update only for a real factual change and must not guess level. UNKNOWN remains distinct from CONFLICT; do not change normalization or writeability.

## Protected baseline

Use canonical `PROSPECT_LEVEL_POPULATION`. Require two matching deterministic pre-Preview captures and a post-Preview `UNCHANGED` comparison for protected domains and prospect evidence.

## Data Health and canonical input

Run Data Health only after diagnostic gates pass. Record current failures/warnings and whether any warning blocks G1. Inspect representative canonical Player Intelligence input from every available category, including UUID, display name, MLBAM, `isMinorLeaguer`, `currentLevel`, `levelSource`, `levelAvailability`, `levelObservedAt`, freshness, and evidence availability. Require factual evidence without inference from names, age, reputation, HKB, Fantrax production, or Statcast.

## Stop conditions

Stop without patching production behavior on any residual false update, diagnostic insufficiency, identity contradiction, Preview/protected mutation, schema/migration/authentication/league contradiction, blocking Data Health failure, canonical-input defect, or restoration failure. Any repair requires a separate task.

## Completion status

If every gate passes: G0I/G0H/G0G/G0F/G0A are complete/checkpointed; G1 is **UNBLOCKED FOR LOCAL IMPLEMENTATION**; calibration remains `CALIBRATION_REQUIRED`; V5.5C remains blocked. If any gate fails, preserve evidence and keep G1 blocked.

## Documentation

Preserve history in `docs/NEXT_TASK_RESULT.md`. Record deployment/restoration, authenticated league, schema/population proof, baselines, Preview/reason counts, every update classification, representative no-op/conflict/unknown evidence, post-Preview comparison, Data Health, canonical inputs, G1 decision, and confirmation that Review/Apply and data mutation did not occur. Do not automatically begin G1.
